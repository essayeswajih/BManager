import { CounterUpDirective } from './counter-up.directive';

describe('CounterUpDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterUpDirective();
    expect(directive).toBeTruthy();
  });
});
